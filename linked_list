class LinkedList:
    def _init_(self):
        self.head = None
        self.tail = None
    def is_empty(self):
        if self.head:
            return False
        else
            return True
    def insert_front(self, node):
        if self.is_empty():
            self.head = node
            self.tail = node
        else:
            self.head= newnode.nextnode



